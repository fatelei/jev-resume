{
  "name": "jev-resume",
  "private": true,
  "version": "0.1.1",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "test": "npm test --prefix crates/jev-app/ui",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.5"
  }
}
